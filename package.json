{
  "name": "company-website-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Starter structure for a company website using HTML, Sass, Bootstrap, JavaScript, and Material Design Icons.",
  "scripts": {
    "sass": "sass --watch assets/sass:assets/css",
    "build:css": "sass assets/sass:assets/css --style=compressed"
  },
  "devDependencies": {
    "sass": "^1.89.0"
  }
}
